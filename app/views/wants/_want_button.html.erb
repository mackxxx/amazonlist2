<% if current_user.want?(item) %>
  <%= link_to 'いらない', item_want_path(item.id), class: 'btn btn-danger', method: :delete %>
<% else %>
  <%= link_to '今欲しい', item_wants_path, class: 'btn btn-primary', method: :post %>
<% end %>