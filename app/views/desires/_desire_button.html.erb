<% if current_user.desire?(item.code) %>
  <%= link_to 'まだいらない', item_desire_path, class: 'btn btn-danger', method: :delete %>
<% else %> 
  <%= link_to '後で欲しい', item_desires_path(item.code), class: 'btn btn-primary', method: :post %>
<% end %>