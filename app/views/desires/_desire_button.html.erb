<% if current_user.desire?(item) %>
  <%= link_to 'まだいらない', item_desire_path(item.id), class: 'btn btn-danger', method: :delete %>
<% else %> 
  <%= link_to '後で欲しい', item_desires_path, class: 'btn btn-primary', method: :post %>
<% end %>