<% content_for :cover do %>
  <div class="cover">
    <div class="cover-inner">
      <div class="cover-contents">
        <h1>探しものは何ですか</h1>
        <% unless logged_in? %>
          <%= link_to "商品リストを始める", signup_path, class: "btn btn-outline-primary" %>
        <% end %>
      </div>
    </div>
  </div>
<% end %>

<%= render 'items/items', items: @items %>